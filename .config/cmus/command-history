add wynZFS/Media/Audio/Music/
add wynZFS/Media/Audio/Music (lossy)/
clear
update-cache -f
update-cache 
add /home/wynand/wynZFS/Media/Audio/Music/
add /home/wynand/wynZFS/Media/Audio/Music (lossy)/
bind -f common right seek +5
set color_win_attr=defaultl
bind -f common , seek -1mjjjj
bind -f browser space win-activate
bind -f common f player-next
bind -f common a player-prev
bind -f common d seek +5
bind -f common s seek -5
bind -f common a player-next
bind -f common g player-pause
bind -f common g win-top
bind -f common G win-top
bind -f common gg win-top
bind -f common space win-activate
bind -f common y seek +5
bind -f common x seek -5
bind -f common x player-prev
bind -f common z player-prev
bind -f common v player-next
bind -f common v player-pause
bind -f common b player-pause
bind -f common c player-next
bind -f common z player-play
bind -f common space win-toggle
bind -f common space win activate
bind -f common space player-toggle
bind -f common h win-next
bind -f common h win-pre
bind -f common h win-previous
bind -f common h win-prev
bind -f common l win-next
live-filter 2
